openapi: 3.0.0
info:
  title: StudySync API
  version: 1.0.0
  description: A collaborative study management tool API for tracking tasks, deadlines, activity, and study materials
  contact:
    name: StudySync Team
  license:
    name: MIT

servers:
  - url: http://localhost:5000
    description: Development server
  - url: https://api.studysync.example.com
    description: Production server

tags:
  - name: Members
    description: Team member information and reliability scores
  - name: Tasks
    description: Task management and status tracking
  - name: Deadlines
    description: Deadline and calendar event management
  - name: Activity
    description: User activity history and study logs
  - name: Study Materials
    description: Flashcard decks and study materials

paths:
  /api/members:
    get:
      tags:
        - Members
      summary: Get all team members with group information
      description: Retrieves a list of all team members including their reliability scores and group assignments
      operationId: getMembers
      responses:
        '200':
          description: Successfully retrieved list of members
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Member'
              example:
                - id: anna
                  name: Anna
                  initials: A
                  reliability: 95
                  group_id: g1
                  group_name: Study Group Alpha
                - id: ben
                  name: Ben
                  initials: B
                  reliability: 80
                  group_id: g1
                  group_name: Study Group Alpha
                - id: clara
                  name: Clara
                  initials: C
                  reliability: 90
                  group_id: g1
                  group_name: Study Group Alpha
        '500':
          description: Server error - unable to fetch members
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: Failed to fetch members

  /users/{userId}/reliability-score:
    get:
      tags:
        - Members
      summary: Get reliability score for a specific user
      description: Returns the reliability score for a user, which indicates their track record of completing tasks on time
      operationId: getUserReliabilityScore
      parameters:
        - name: userId
          in: path
          required: true
          description: The unique identifier of the user
          schema:
            type: string
          example: anna
      responses:
        '200':
          description: Successfully retrieved reliability score
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ReliabilityScore'
              example:
                user_id: anna
                user_name: Anna
                reliability_score: 95
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: User not found
        '500':
          description: Server error - unable to fetch reliability score
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: Failed to fetch reliability score

  /tasks:
    get:
      tags:
        - Tasks
      summary: Get tasks with optional filtering
      description: Retrieves all tasks, optionally filtered by status, group, or assignee
      operationId: getTasks
      parameters:
        - name: status
          in: query
          required: false
          description: Filter tasks by status (pending, done, late)
          schema:
            type: string
            enum:
              - pending
              - done
              - late
          example: pending
        - name: groupId
          in: query
          required: false
          description: Filter tasks by group ID
          schema:
            type: string
          example: g1
        - name: assignee
          in: query
          required: false
          description: Filter tasks by assignee (member ID)
          schema:
            type: string
          example: anna
      responses:
        '200':
          description: Successfully retrieved list of tasks
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Task'
              example:
                - id: t1
                  title: Finish calculus problem set
                  assigned_to: anna
                  status: pending
                  deadline: "2026-09-12T23:59:00Z"
                - id: t2
                  title: Read chapter 6 notes
                  assigned_to: ben
                  status: late
                  deadline: "2026-09-05T23:59:00Z"
                - id: t3
                  title: Prepare flashcards for quiz
                  assigned_to: clara
                  status: done
                  deadline: "2026-09-08T18:00:00Z"
        '400':
          description: Invalid filter parameters
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: Invalid status value. Must be pending, done, or late
        '500':
          description: Server error - unable to fetch tasks
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: Failed to fetch tasks

  /calendar-events:
    get:
      tags:
        - Deadlines
      summary: Get deadlines and calendar events in a date range
      description: Retrieves all deadlines/calendar events between specified dates, useful for calendar and timeline views
      operationId: getCalendarEvents
      parameters:
        - name: from
          in: query
          required: true
          description: Start date for the range (ISO 8601 format)
          schema:
            type: string
            format: date-time
          example: "2026-09-01T00:00:00Z"
        - name: to
          in: query
          required: true
          description: End date for the range (ISO 8601 format)
          schema:
            type: string
            format: date-time
          example: "2026-09-30T23:59:59Z"
      responses:
        '200':
          description: Successfully retrieved calendar events in the date range
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Deadline'
              example:
                - id: t2
                  title: Read chapter 6 notes
                  assigned_to: ben
                  status: late
                  deadline: "2026-09-05T23:59:00Z"
                - id: t3
                  title: Prepare flashcards for quiz
                  assigned_to: clara
                  status: done
                  deadline: "2026-09-08T18:00:00Z"
        '400':
          description: Invalid date range parameters
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: Invalid date format. Use ISO 8601 format (e.g., 2026-09-01T00:00:00Z)
        '500':
          description: Server error - unable to fetch calendar events
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: Failed to fetch calendar events

  /deadlines:
    get:
      tags:
        - Deadlines
      summary: Get all upcoming deadlines sorted by date
      description: Retrieves all tasks with deadlines, sorted in ascending order by deadline date
      operationId: getDeadlines
      responses:
        '200':
          description: Successfully retrieved list of deadlines
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Deadline'
              example:
                - id: t2
                  title: Read chapter 6 notes
                  assigned_to: ben
                  status: late
                  deadline: "2026-09-05T23:59:00Z"
                - id: t3
                  title: Prepare flashcards for quiz
                  assigned_to: clara
                  status: done
                  deadline: "2026-09-08T18:00:00Z"
                - id: t1
                  title: Finish calculus problem set
                  assigned_to: anna
                  status: pending
                  deadline: "2026-09-12T23:59:00Z"
        '500':
          description: Server error - unable to fetch deadlines
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: Failed to fetch deadlines

  /users/{userId}/activity:
    get:
      tags:
        - Activity
      summary: Get activity history for a specific user
      description: Retrieves the study/work activity log for a user, showing when they studied and for how long
      operationId: getUserActivity
      parameters:
        - name: userId
          in: path
          required: true
          description: The unique identifier of the user
          schema:
            type: string
          example: anna
      responses:
        '200':
          description: Successfully retrieved user activity history
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ActivityLog'
              example:
                - member_id: anna
                  activity_date: "2026-09-07T00:00:00Z"
                  minutes_studied: 30
                - member_id: anna
                  activity_date: "2026-09-06T00:00:00Z"
                  minutes_studied: 45
        '404':
          description: User not found
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: User not found
        '500':
          description: Server error - unable to fetch activity data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: Failed to fetch activity data

  /activity:
    get:
      tags:
        - Activity
      summary: Get activity log for all users or filtered by member
      description: Retrieves activity logs, optionally filtered by a specific member. Useful for team-wide activity tracking and heatmaps
      operationId: getActivity
      parameters:
        - name: member
          in: query
          required: false
          description: Filter activity by member ID
          schema:
            type: string
          example: anna
      responses:
        '200':
          description: Successfully retrieved activity logs
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ActivityLog'
              example:
                - member_id: anna
                  activity_date: "2026-09-07T00:00:00Z"
                  minutes_studied: 30
                - member_id: ben
                  activity_date: "2026-09-06T00:00:00Z"
                  minutes_studied: 10
                - member_id: clara
                  activity_date: "2026-09-07T00:00:00Z"
                  minutes_studied: 60
        '400':
          description: Invalid member filter parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: Invalid member ID format
        '500':
          description: Server error - unable to fetch activity data
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: Failed to fetch activity data

  /flashcard-decks:
    get:
      tags:
        - Study Materials
      summary: Get all flashcard decks, optionally filtered by group
      description: Retrieves all flashcard decks with their cards, optionally filtered to a specific group
      operationId: getFlashcardDecks
      parameters:
        - name: groupId
          in: query
          required: false
          description: Filter decks by group ID
          schema:
            type: string
          example: g1
      responses:
        '200':
          description: Successfully retrieved flashcard decks with cards
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/FlashcardDeck'
              example:
                - id: d1
                  title: Data Structures Basics
                  owner_id: anna
                  shared_with_group: g1
                  cards:
                    - id: f1
                      deck_id: d1
                      question: What is a hash table?
                      answer: A data structure that maps keys to values for O(1) average lookup
                    - id: f2
                      deck_id: d1
                      question: What is Big-O notation?
                      answer: A way of describing an algorithm's worst-case complexity
        '400':
          description: Invalid group ID parameter
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: Invalid group ID format
        '500':
          description: Server error - unable to fetch flashcard decks
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Error'
              example:
                error: Failed to fetch flashcards

components:
  schemas:
    Member:
      type: object
      description: A team member with reliability tracking
      required:
        - id
        - name
        - initials
        - reliability
      properties:
        id:
          type: string
          description: Unique identifier for the member
          example: anna
        name:
          type: string
          description: Full name of the member
          example: Anna
        initials:
          type: string
          description: Member's initials for display purposes
          example: A
          minLength: 1
          maxLength: 5
        reliability:
          type: integer
          description: Reliability score (0-100) indicating track record of completing tasks on time
          example: 95
          minimum: 0
          maximum: 100
        group_id:
          type: string
          description: ID of the group this member belongs to (nullable)
          example: g1
          nullable: true
        group_name:
          type: string
          description: Name of the group this member belongs to (nullable)
          example: Study Group Alpha
          nullable: true

    ReliabilityScore:
      type: object
      description: Reliability score information for a user
      required:
        - user_id
        - user_name
        - reliability_score
      properties:
        user_id:
          type: string
          description: Unique identifier for the user
          example: anna
        user_name:
          type: string
          description: Full name of the user
          example: Anna
        reliability_score:
          type: integer
          description: Reliability score (0-100) indicating track record of completing tasks on time
          example: 95
          minimum: 0
          maximum: 100

    Task:
      type: object
      description: A task or to-do item assigned to a team member
      required:
        - id
        - title
        - assigned_to
        - status
        - deadline
      properties:
        id:
          type: string
          description: Unique identifier for the task
          example: t1
        title:
          type: string
          description: Title/description of the task
          example: Finish calculus problem set
        assigned_to:
          type: string
          description: ID of the team member this task is assigned to
          example: anna
        status:
          type: string
          description: Current status of the task
          enum:
            - pending
            - done
            - late
          example: pending
        deadline:
          type: string
          format: date-time
          description: Deadline for completing the task (ISO 8601 format)
          example: "2026-09-12T23:59:00Z"

    Deadline:
      type: object
      description: A deadline or calendar event with associated task information
      required:
        - id
        - title
        - assigned_to
        - status
        - deadline
      properties:
        id:
          type: string
          description: Unique identifier for the deadline/task
          example: t1
        title:
          type: string
          description: Title of the deadline/task
          example: Finish calculus problem set
        assigned_to:
          type: string
          description: ID of the team member responsible
          example: anna
        status:
          type: string
          description: Current status of the task
          enum:
            - pending
            - done
            - late
          example: pending
        deadline:
          type: string
          format: date-time
          description: The deadline date and time (ISO 8601 format)
          example: "2026-09-12T23:59:00Z"

    ActivityLog:
      type: object
      description: A record of study or work activity for a team member
      required:
        - member_id
        - activity_date
        - minutes_studied
      properties:
        member_id:
          type: string
          description: ID of the team member who completed this activity
          example: anna
        activity_date:
          type: string
          format: date-time
          description: When the activity occurred (ISO 8601 format)
          example: "2026-09-07T00:00:00Z"
        minutes_studied:
          type: integer
          description: Duration of the activity in minutes
          example: 30
          minimum: 0

    Flashcard:
      type: object
      description: A single flashcard with a question and answer
      required:
        - id
        - deck_id
        - question
        - answer
      properties:
        id:
          type: string
          description: Unique identifier for the flashcard
          example: f1
        deck_id:
          type: string
          description: ID of the deck this card belongs to
          example: d1
        question:
          type: string
          description: The question side of the flashcard
          example: What is a hash table?
        answer:
          type: string
          description: The answer side of the flashcard
          example: A data structure that maps keys to values for O(1) average lookup

    FlashcardDeck:
      type: object
      description: A deck of flashcards with all associated cards
      required:
        - id
        - title
        - owner_id
        - cards
      properties:
        id:
          type: string
          description: Unique identifier for the deck
          example: d1
        title:
          type: string
          description: Title of the flashcard deck
          example: Data Structures Basics
        owner_id:
          type: string
          description: ID of the user who created this deck
          example: anna
        shared_with_group:
          type: string
          description: ID of the group this deck is shared with, derived from the owner's group when the deck is marked shared (nullable)
          example: g1
          nullable: true
        cards:
          type: array
          description: Array of all flashcards in this deck
          items:
            $ref: '#/components/schemas/Flashcard'

    Error:
      type: object
      description: Standard error response
      required:
        - error
      properties:
        error:
          type: string
          description: Human-readable error message
          example: Failed to fetch members
